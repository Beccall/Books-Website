<ul>
    {% for book in books %}
        <li>
            <a href="{{ url_for('book', book_id=searched_item.id)}}">
                {{book.isbn}}: {{book.title}}, written by: {{book.author}} , year: {{book.year}}
            </a>
        </li>
    {% endfor %}
</ul>
<div class="form-group">
    <select class="form-control" name="book_id">
        {% for book in books %}
            {{ url_for('book', book_id=book.id)}}
            <option value ="{{book.id}}"> {{book.isbn}}: {{book.title}}, written by: {{book.author}} , year: {{book.year}} </option>
            </a>
        {% endfor %}
    </select>
</div>
<div class="form-group">
    <button Class="btn btn-primary"> Select Book </button>
</div>

<a href="{{ url_for('book', book_id=searched_item.id)}}">
<a href="{{ url_for('book', book=book)}}"> {{book_info}} </a>


SELECT * FROM books WHERE isbn like '%1982%' OR title like '%1982%' OR author like '%1982%' OR year like '%1982%';

ALTER TABLE books ALTER COLUMN year TYPE varchar;
